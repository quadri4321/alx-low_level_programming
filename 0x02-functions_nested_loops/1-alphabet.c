#include "main.h"
/**
 * main - check the code
 * C program to prints the alphabet, in lowercase, followed by a new line
 * Return: Always 0.
**/
int main(void)
{
int ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
_putchar("\n");
}
return (0);
}
